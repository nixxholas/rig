# Rig and Happy documentation

This folder ships with Rig itself and is exposed read-only to agents: on the
local host at the installed package's `dist/docs` folder, and inside managed
Docker containers at `/happy/docs`. It documents Rig and Happy.
