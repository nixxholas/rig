# Folder item persistence

Folder items are ordered links owned by exactly one folder.

```text
folder -> ordered folder_items -> project | workspace | document
```

Mutating or archiving a link never mutates its target.
