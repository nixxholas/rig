# Imported source helpers

Shared, bounded primitives for copying user-selected source trees and producing validated icon
artifacts. Applets and worklets use these helpers so file-count, byte, symlink, special-file, and
image rules stay identical.

Feature-specific folders remain responsible for manifests, documents, version layout, persistence,
and lifecycle behavior.
