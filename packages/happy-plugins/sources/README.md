# Sources

This directory contains the public extension types and the Unix-socket client that implements
them. `index.ts` is the package boundary; transport details stay in `createHappyPluginClient.ts`.
