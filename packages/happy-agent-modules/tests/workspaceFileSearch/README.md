# Workspace file-search tests

These tests exercise the native fuzzy index through the module's public surface. They cover legacy
fuzzy path matching and the empty query used when the composer contains a bare `@`.
